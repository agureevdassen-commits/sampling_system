# Система учёта геологических проб - PWA + REST API

Полнофункциональное решение для учёта геологических проб с поддержкой офлайн-работы, сканирования штрихкодов и синхронизацией с сервером.

## 📋 Содержание решения

- **index.html** – полностью функциональное PWA-приложение на чистом JavaScript
- **manifest.json** – веб-манифест для установки как приложение
- **service-worker.js** – сервис-воркер для кэширования и офлайн-работы
- **server.js** – Node.js/Express API сервер
- **001_init_schema.sql** – SQL миграции для инициализации БД
- **package.json** – зависимости Node.js
- **.env.example** – пример переменных окружения

## 🚀 Быстрый старт

### Требования
- Node.js >= 16
- PostgreSQL >= 12
- Современный браузер Chrome/Edge (для сканирования камеры)

### Установка и запуск (локально)

#### 1. Подготовка переменных окружения
```bash
cp .env.example .env
# Отредактируйте .env и установите DATABASE_URL
```

#### 2. Установка зависимостей
```bash
npm install
```

#### 3. Инициализация БД
```bash
# Создайте БД PostgreSQL
createdb sampling_db

# Запустите миграции
psql sampling_db -f migrations/001_init_schema.sql

# Или используйте npm скрипт:
npm run db:migrate
```

#### 4. Запуск сервера
```bash
# Разработка (с автоперезагрузкой)
npm run dev

# Продакшен
npm start
```

Сервер будет доступен на `http://localhost:3000`

### Использование приложения

1. **Откройте в браузере**: `http://localhost:3000`
2. **Установите как PWA**: 
   - Chrome: адресная строка → иконка установки → "Установить"
   - Или: меню → "Установить приложение"
3. **Работа в офлайне**: Все данные сохраняются локально в IndexedDB
4. **Синхронизация**: При наличии интернета данные автоматически отправляются на сервер

## 🏗 Структура проекта

```
.
├── public/
│   ├── index.html              # PWA приложение
│   ├── manifest.json           # Веб-манифест
│   └── service-worker.js       # Сервис-воркер
├── migrations/
│   └── 001_init_schema.sql     # SQL миграции
├── server.js                   # Node.js/Express сервер
├── package.json                # Зависимости
├── .env.example                # Пример конфигурации
└── README.md                   # Документация
```

## 🔌 API Endpoints

### POST /api/scans/bulk
Приём пакета проб от клиента

**Заголовок**: `X-API-Key: <API_KEY>`

**Тело запроса**:
```json
[
  {
    "device_id": "device-123",
    "sample": "S001",
    "well_name": "SW-001",
    "block": "B01",
    "type": "520",
    "scanned_at": 1703050800000,
    "scanned_by": "Иван",
    "is_test": false,
    "local_id": 1
  }
]
```

**Ответ успеха** (200):
```json
{
  "server_ids": [1, 2, 3],
  "total_on_server": 150
}
```

### GET /api/scans/count
Получить общее количество проб на сервере

**Заголовок**: `X-API-Key: <API_KEY>`

**Ответ**:
```json
{
  "total_on_server": 150
}
```

### GET /api/scans
Получить список проб с фильтрацией

**Параметры**:
- `device_id` – фильтр по ID устройства
- `start_date` – начальная дата (ISO 8601)
- `end_date` – конечная дата (ISO 8601)
- `limit` – максимум записей (по умолчанию 1000)

**Ответ**:
```json
{
  "data": [...],
  "count": 50
}
```

### GET /api/scans/export/csv
Экспортировать все пробы в CSV

**Заголовок**: `X-API-Key: <API_KEY>`

**Ответ**: CSV файл для скачивания

### GET /api/health
Проверка состояния сервера

**Ответ**:
```json
{
  "status": "ok",
  "timestamp": "2025-12-20T20:00:00Z"
}
```

## 🔐 Безопасность

### API Key
Все запросы к API должны содержать заголовок:
```
X-API-Key: sampling-dev-key-2025
```

Измените значение в `.env` файле перед развёртыванием на продакшене.

### HTTPS
В продакшене используйте HTTPS для всех соединений.

### CORS
CORS настроен на все источники по умолчанию. Ограничьте в продакшене:
```javascript
cors({
  origin: process.env.CLIENT_URL,
  credentials: true
})
```

## 💾 Локальное хранилище (IndexedDB)

Фронтенд использует IndexedDB с следующей структурой:

**Database**: `sampling_scans_db_v2`

**Object Store: scans**
```javascript
{
  id: number,
  sample: string,
  well_name: string,
  block: string,
  type: string,
  scanned_at: number,
  scanned_by: string,
  sync_status: 'pending' | 'synced',
  server_id: string,
  is_test: boolean
}
```

**Object Store: logs**
```javascript
{
  id: number,
  ts: number,
  level: 'info' | 'error',
  message: string
}
```

## 📊 Развёртывание на продакшене

### Heroku
```bash
# Создать приложение
heroku create <app-name>

# Добавить PostgreSQL
heroku addons:create heroku-postgresql:hobby-dev

# Установить переменные окружения
heroku config:set NODE_ENV=production
heroku config:set API_KEY=<your-secure-key>

# Отправить код
git push heroku main

# Запустить миграции
heroku run npm run db:migrate
```

### Docker
```bash
docker build -t sampling-system .
docker run -e DATABASE_URL=<url> -p 3000:3000 sampling-system
```

### AWS/GCP/DigitalOcean
1. Создайте PostgreSQL экземпляр
2. Развёртите Node.js приложение через:
   - AWS Elastic Beanstalk
   - Google App Engine
   - DigitalOcean App Platform
3. Установите переменные окружения
4. Запустите миграции БД

## 🐛 Отладка

### Просмотр логов сервера
```bash
tail -f server.log
```

### Проверка соединения БД
```bash
psql $DATABASE_URL -c "SELECT COUNT(*) FROM samples;"
```

### Тестирование API
```bash
curl -X GET http://localhost:3000/api/health
```

### Клиентская диагностика
Откройте приложение → кнопка ⚙ "Диагностика" → просмотр логов и очистка

## 📱 Особенности PWA

- **Offline-first**: Приложение работает без интернета
- **Синхронизация**: Автоматическая синхронизация при подключении
- **Установка**: Работает как нативное приложение
- **Кэширование**: Статические ресурсы кэшируются для быстрой загрузки
- **Поддержка темной темы**: Оптимизировано для ночной работы на буровой

## 📝 Поддерживаемые форматы скана

Приложение распознаёт штрихкоды:
- Code128
- QR Code
- EAN-13
- UPC-A
- и другие форматы, поддерживаемые ZXing

## 🔄 Синхронизация

Приложение периодически синхронизирует pending записи:
- Каждые 30 секунд при наличии интернета
- Сразу после сохранения записи (если онлайн)
- Статус отображается в индикаторе синхронизации

## 🎨 Кастомизация

### Изменить типы руды
Отредактируйте в `index.html` массив `ORE_TYPES`:
```javascript
const ORE_TYPES = ['520', '360', 'Ваш тип'];
```

### Изменить интервал синхронизации
В `index.html` найдите и измените:
```javascript
setInterval(syncPendingScans, 30000); // Значение в миллисекундах
```

### Добавить собственный логирующий сервис
Отредактируйте функцию `addLog()` в `index.html`

## 📄 Лицензия

MIT

## 📞 Поддержка

Для вопросов и проблем обратитесь в документацию проекта.

---

**Версия**: 1.0.0
**Дата**: Декабрь 2025
